% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-advanced.R
\name{notchFilter}
\alias{notchFilter}
\title{Notch filter (power line noise removal)}
\usage{
notchFilter(
  x,
  freq = 50,
  bandwidth = 2,
  harmonics = 1L,
  output_assay = "filtered"
)
}
\arguments{
\item{x}{A \code{PhysioExperiment} object.}

\item{freq}{Center frequency to remove in Hz. Default is 50 (European power line).}

\item{bandwidth}{Bandwidth of the notch in Hz. Default is 2.}

\item{harmonics}{Number of harmonics to remove. Default is 1 (only fundamental).}

\item{output_assay}{Name for the output assay. Default is "filtered".}
}
\value{
A \code{PhysioExperiment} object with a new assay named \code{output_assay}
containing the notch-filtered data. Harmonics above the Nyquist frequency
are skipped with a warning.
}
\description{
Applies a notch filter to remove power line noise (50 Hz or 60 Hz) and
optionally its harmonics. Implemented as a Butterworth bandstop filter
with zero-phase filtering.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000 * 4), nrow = 1000)),
  samplingRate = 250
)

# Remove 50 Hz power line noise (Europe/Asia)
pe <- notchFilter(pe, freq = 50)

# Remove 60 Hz and harmonics (Americas)
pe <- notchFilter(pe, freq = 60, harmonics = 2)
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=butterworthFilter]{butterworthFilter()}} for general Butterworth filtering,
\code{\link[=firFilter]{firFilter()}} for FIR filtering, \code{\link[=filterSignals]{filterSignals()}} for moving average
filtering.
}
