% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{resample}
\alias{resample}
\title{Resampling operations for PhysioExperiment}
\usage{
resample(
  x,
  target_rate,
  method = c("linear", "spline", "fft"),
  assay_name = NULL,
  output_assay = "resampled"
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{target_rate}{Target sampling rate in Hz.}

\item{method}{Resampling method: "linear" (default), "spline", or "fft".}

\item{assay_name}{Optional assay name. If NULL, uses the default assay.}

\item{output_assay}{Name for the output assay. Default is "resampled".}
}
\value{
A new \code{PhysioExperiment} object with sampling rate set to
\code{target_rate}. The time dimension is adjusted to match the new rate
while preserving the signal duration. Column data and metadata are
carried over from the input.
}
\description{
Functions for resampling signal data to different sampling rates.
Resample signal data
}
\details{
Resamples the signal data to a target sampling rate using interpolation.
Supports linear interpolation, spline interpolation, and FFT-based
resampling (zero-padding or truncation in the frequency domain).
}
\references{
Crochiere, R.E. & Rabiner, L.R. (1983). "Multirate Digital
Signal Processing." Prentice Hall.
}
\seealso{
\code{\link[=decimate]{decimate()}} for integer-factor downsampling with anti-aliasing,
\code{\link[=interpolate]{interpolate()}} for integer-factor upsampling,
\code{\link[=setAssaySamplingRate]{setAssaySamplingRate()}} for per-assay rate tracking.
}
