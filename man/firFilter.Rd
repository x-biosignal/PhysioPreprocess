% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-advanced.R
\name{firFilter}
\alias{firFilter}
\title{FIR filter}
\usage{
firFilter(
  x,
  low = NULL,
  high = NULL,
  order = 100L,
  type = c("pass", "low", "high", "stop"),
  window = "hamming",
  causal = FALSE,
  output_assay = "filtered"
)
}
\arguments{
\item{x}{A \code{PhysioExperiment} object.}

\item{low}{Lower cutoff frequency in Hz.}

\item{high}{Upper cutoff frequency in Hz.}

\item{order}{Filter order (number of taps - 1). Default is 100.}

\item{type}{Filter type: "low", "high", "pass" (bandpass), or "stop" (bandstop).}

\item{window}{Window function for FIR design. Default is "hamming".}

\item{causal}{Logical. If FALSE (default), apply zero-phase forward-backward
filtering (\code{\link[signal:filtfilt]{signal::filtfilt()}}), which is non-causal. If TRUE, apply a
single forward (causal) pass via \code{\link[=lfilter]{lfilter()}} with a steady-state warm
start, producing a real-time-equivalent result (linear-phase group delay
of \code{order / 2} samples, no acausal pre-ringing).}

\item{output_assay}{Name for the output assay. Default is "filtered".}
}
\value{
A \code{PhysioExperiment} object with a new assay named \code{output_assay}
containing the FIR-filtered data. Dimensions match the input assay.
}
\description{
Applies a Finite Impulse Response (FIR) filter along the time axis.
Uses zero-phase forward-backward filtering via \code{\link[signal:filtfilt]{signal::filtfilt()}} to
avoid phase distortion.
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=butterworthFilter]{butterworthFilter()}} for IIR filtering, \code{\link[=notchFilter]{notchFilter()}} for power
line noise removal, \code{\link[=filterSignals]{filterSignals()}} for moving average filtering,
\code{\link[=lfilter]{lfilter()}} for the underlying causal filtering primitive.
}
