% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-causal.R
\name{sosfiltInit}
\alias{sosfiltInit}
\title{Steady-state initial conditions for a cascaded SOS filter}
\usage{
sosfiltInit(sos)
}
\arguments{
\item{sos}{SOS matrix (\code{n_sections} x 6).}
}
\value{
A numeric matrix (\code{n_sections} x 2) of per-section initial
states (unscaled).
}
\description{
Per-section analogue of \code{\link{lfilterInit}} for second-order-sections
form; equivalent to \code{scipy.signal.sosfilt_zi}. Each section's steady
state is scaled by the DC gain of all preceding sections so that the whole
cascade starts in steady state when the returned matrix (scaled by the first
sample) is handed to \code{\link{sosfilt}}.
}
\examples{
sos <- sosDesign(high = 40, type = "low", sr = 250)
zi <- sosfiltInit(sos)
}
\seealso{
\code{\link{sosfilt}}, \code{\link{lfilterInit}}
}
