% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-advanced.R
\name{butterworthFilter}
\alias{butterworthFilter}
\title{Advanced signal filtering functions}
\usage{
butterworthFilter(
  x,
  low = NULL,
  high = NULL,
  order = 4L,
  type = c("pass", "low", "high", "stop"),
  output_assay = "filtered"
)
}
\arguments{
\item{x}{A \code{PhysioExperiment} object.}

\item{low}{Lower cutoff frequency in Hz. Required for highpass and bandpass.}

\item{high}{Upper cutoff frequency in Hz. Required for lowpass and bandpass.}

\item{order}{Filter order. Default is 4.}

\item{type}{Filter type: "low", "high", "pass" (bandpass), or "stop" (bandstop).}

\item{output_assay}{Name for the output assay. Default is "filtered".}
}
\value{
A \code{PhysioExperiment} object with a new assay named \code{output_assay}
containing the Butterworth-filtered data. Dimensions match the input assay.
}
\description{
This file provides advanced filtering operations including Butterworth,
FIR, and notch filters for physiological signal processing.
Butterworth filter
}
\details{
Applies a Butterworth filter (lowpass, highpass, bandpass, or bandstop)
along the time axis of the specified assay. Uses zero-phase forward-backward
filtering via \code{\link[signal:filtfilt]{signal::filtfilt()}} to avoid phase distortion.
}
\examples{
# Create example EEG data
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000 * 4), nrow = 1000)),
  samplingRate = 250
)

# Bandpass filter (1-40 Hz) - common for EEG
pe <- butterworthFilter(pe, low = 1, high = 40, type = "pass")

# Lowpass filter (30 Hz)
pe <- butterworthFilter(pe, high = 30, type = "low",
                        output_assay = "lowpass")

# Highpass filter (0.5 Hz) to remove DC drift
pe <- butterworthFilter(pe, low = 0.5, type = "high",
                        output_assay = "highpass")
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=firFilter]{firFilter()}} for FIR filtering, \code{\link[=notchFilter]{notchFilter()}} for power line
noise removal, \code{\link[=filterSignals]{filterSignals()}} for moving average filtering,
\code{\link[=detrendSignal]{detrendSignal()}} for trend removal.
}
