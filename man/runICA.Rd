% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ica.R
\name{runICA}
\alias{runICA}
\title{ICA-Based Artifact Removal for PhysioExperiment}
\usage{
runICA(
  pe,
  n_components = NULL,
  assay_name = NULL,
  method = "fastica",
  max_iter = 200,
  ...
)
}
\arguments{
\item{pe}{A PhysioExperiment object.}

\item{n_components}{Number of components to extract. If NULL, uses all channels.}

\item{assay_name}{Name of the assay to decompose.}

\item{method}{ICA algorithm: "fastica" (default).}

\item{max_iter}{Maximum number of iterations.}

\item{...}{Additional arguments passed to fastICA::fastICA.}
}
\value{
A list with components:
\item{S}{Source matrix (time x components)}
\item{A}{Mixing matrix (channels x components)}
\item{W}{Unmixing matrix (components x channels)}
}
\description{
Functions for Independent Component Analysis (ICA) decomposition
and artifact removal using the fastICA algorithm.
Perform ICA decomposition
}
\details{
Decomposes signals into independent components using the fastICA algorithm.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000), nrow = 100, ncol = 10)),
  colData = S4Vectors::DataFrame(label = paste0("Ch", 1:10)),
  samplingRate = 256
)
\dontrun{
ica_result <- runICA(pe, n_components = 5)
}
}
\references{
Hyvarinen A, Oja E (2000). "Independent component analysis:
algorithms and applications." \emph{Neural Networks}, 13(4-5), 411-430.
}
\seealso{
\code{\link[=removeICAComponents]{removeICAComponents()}} for reconstructing signals after removing
artifact components, \code{\link[=icaDecompose]{icaDecompose()}} for the built-in ICA implementation,
\code{\link[=detectBadChannels]{detectBadChannels()}} for channel-level artifact detection.
}
