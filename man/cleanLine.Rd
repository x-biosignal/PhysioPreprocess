% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineNoise.R
\name{cleanLine}
\alias{cleanLine}
\title{Remove line noise with CleanLine}
\usage{
cleanLine(
  x,
  line_freq = 50,
  bandwidth = 2,
  harmonics = 1,
  p_thresh = 0.01,
  window_sec = 4,
  assay_name = NULL,
  output_assay = "cleanline"
)
}
\arguments{
\item{x}{A PhysioExperiment object with a 2D (time x channels) assay.}

\item{line_freq}{Line frequency in Hz (default: 50).}

\item{bandwidth}{Reserved spectral bandwidth in Hz around the line
(default: 2).}

\item{harmonics}{Number of harmonics to remove, including the fundamental
(default: 1, i.e. \code{line_freq} only; 3 removes 50/100/150 Hz).}

\item{p_thresh}{F-test significance threshold for subtracting the line
(default: 0.01).}

\item{window_sec}{Sliding-window length in seconds (default: 4).}

\item{assay_name}{Input assay (default: \code{defaultAssay(x)}).}

\item{output_assay}{Name for the cleaned assay (default: "cleanline").}
}
\value{
The PhysioExperiment with the cleaned signal in \code{output_assay};
the removed line-power fraction is stored in \code{metadata(x)$line_noise}
and logged in provenance.
}
\description{
Removes power-line noise (and optional harmonics) by sliding-window
sinusoidal regression with a Thomson F-test (the CleanLine / Chronux
approach; Mitra & Bokil, 2008). In each Hann-overlapped window each channel is
regressed onto the line sine/cosine pair; when the fit is significant
(\code{p < p_thresh}) the fitted sinusoid is subtracted. This attenuates the
line deeply without a wide spectral notch and works on any number of channels.
}
\references{
Mitra, P., & Bokil, H. (2008). \emph{Observed Brain Dynamics}. Oxford
University Press.
}
\seealso{
\code{\link{zapLine}}, \code{\link{notchFilter}} (the fast fixed
notch)
}
