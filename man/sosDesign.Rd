% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-causal.R
\name{sosDesign}
\alias{sosDesign}
\title{Design a Butterworth filter in second-order-sections (SOS) form}
\usage{
sosDesign(
  low = NULL,
  high = NULL,
  order = 4L,
  type = c("pass", "low", "high", "stop"),
  sr
)
}
\arguments{
\item{low}{Lower cutoff frequency in Hz. Required for \code{"high"} and
\code{"pass"}/\code{"stop"}.}

\item{high}{Upper cutoff frequency in Hz. Required for \code{"low"} and
\code{"pass"}/\code{"stop"}.}

\item{order}{Filter order (per band). Default \code{4}.}

\item{type}{Filter type: \code{"low"}, \code{"high"}, \code{"pass"}
(bandpass), or \code{"stop"} (bandstop).}

\item{sr}{Sampling rate in Hz.}
}
\value{
A numeric matrix with one second-order section per row and six
columns \code{c(b0, b1, b2, a0, a1, a2)}.
}
\description{
Builds the cascaded second-order-sections representation of a Butterworth
filter directly from the analog prototype poles (bilinear transform), which
is numerically stable for all orders. This is the design entry point for the
causal / stateful filtering functions (\code{\link{sosfilt}},
\code{\link{sosfiltfilt}}, \code{\link{StreamFilter}}).
}
\examples{
sos <- sosDesign(high = 40, type = "low", sr = 250, order = 4)
y <- sosfilt(sos, rnorm(1000))
}
\seealso{
\code{\link{sosfilt}}, \code{\link{sosfiltfilt}},
\code{\link{StreamFilter}}
}
